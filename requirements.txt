# ============================================================
# DiaLong-AutoML — pinned/bounded runtime dependencies
# Generated for Phase 1; extend as later phases add components
# ============================================================

# --- Core configuration & environment ---
pydantic>=2.7,<3.0
pyyaml>=6.0.1,<7.0
python-dotenv>=1.0.0,<2.0

# --- Data / numerical ---
numpy>=1.26,<3.0
pandas>=2.2,<3.0

# --- Machine learning ---
scikit-learn>=1.5,<2.0
xgboost>=2.0,<3.0
lightgbm>=4.3,<5.0

# --- AutoML / hyperparameter optimisation ---
optuna>=3.6,<5.0

# --- Explainability ---
shap>=0.45,<1.0

# --- Deep learning (GRU, TCN planned) ---
torch>=2.3,<3.0

# --- API layer ---
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0

# --- CLI / terminal output ---
rich>=13.7,<14.0

# ============================================================
# Development / testing (install via: pip install -e ".[dev]")
# ============================================================
# pytest>=8.2,<9.0
# pytest-cov>=5.0,<6.0
# ruff>=0.4,<1.0
# mypy>=1.10,<2.0
# pre-commit>=3.7,<4.0
